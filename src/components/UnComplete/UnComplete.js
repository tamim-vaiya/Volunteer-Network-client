import React from 'react';
import Header from '../Header/Header';

const UnComplete = () => {
  return (
    <div className="App">
      <Header></Header>
      <h1 style={{marginTop: '40px'}}>This page isn't completed yet</h1>
    </div>
  );
};

export default UnComplete;